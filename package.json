{
	"scripts": {
		"dev": "bun wrangler dev src/app.tsx --live-reload --env=development",
		"postinstall": "bun run bknd copy-assets --out public",
		"cf:typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"bknd": "^0.19.0",
		"mono-jsx": "^0.8.0-beta.4"
	},
	"devDependencies": {
		"wrangler": "^4.54.0"
	}
}
